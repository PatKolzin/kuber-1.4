apiVersion: v1
kind: Pod
metadata:
   name: multitool
   namespace: network-1
spec:
   containers:
     - name: multitool
       image: wbitt/network-multitool
       ports:
        - containerPort: 8080
